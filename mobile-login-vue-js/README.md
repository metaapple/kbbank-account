# Mobile Login Screen - Vue + ECMAScript

## 실행 방법

```bash
npm install
npm run dev
```

## 변경 내용
- React + TSX -> Vue 3 + App.vue
- TypeScript -> ECMAScript JavaScript
- useState -> ref
- onSubmit/onChange -> @submit / v-model
